import { ConsultaResponseModel } from './consulta-response';

describe('ConsultaResponse', () => {
  it('should create an instance', () => {
    expect(new ConsultaResponseModel()).toBeTruthy();
  });
});
