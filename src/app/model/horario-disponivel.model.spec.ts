import { HorarioDisponivelModel } from './horario-disponivel.model';

describe('HorarioDisponivelModel', () => {
  it('should create an instance', () => {
    expect(new HorarioDisponivelModel()).toBeTruthy();
  });
});
