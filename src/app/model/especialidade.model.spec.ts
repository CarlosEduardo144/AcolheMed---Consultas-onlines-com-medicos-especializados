import { EspecialidadeModel } from './especialidade.model';

describe('EspecialidadeModel', () => {
  it('should create an instance', () => {
    expect(new EspecialidadeModel()).toBeTruthy();
  });
});
